# Scheduler skill (local stub)

Manage watering reminders, feeding schedules, pruning dates, and other recurring
gardening care tasks.

> This is a minimal local stub created by the deploy-time skill installer as a
> fallback when the ClawHub `scheduler` skill could not be resolved. Replace it
> with the real skill (or let the installer overwrite it) when available.

## Capabilities
- Track recurring care tasks per plant.
- Surface upcoming and overdue reminders.
